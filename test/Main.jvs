void main()
{
   int n = readInt();
   assert (n == 42);
   char c = readChar();
   assert (c == 'a');
   c = readChar();
   assert (c == 'b');
   // Print nothing
   println();
   println("Everything is good !");
}
